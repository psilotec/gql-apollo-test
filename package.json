{
  "name": "gql-apollo-test",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {},
  "dependencies": {
    "graphql": "^0.9.1",
    "graphql-server-express": "^0.5.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
